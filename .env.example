VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
API_FOOTBALL_KEY=

# Usada por api/narrar-evento.js (serverless, backend). NUNCA exponer al
# cliente/bundle (por eso no lleva prefijo VITE_).
GEMINI_API_KEY=

# Usadas por scripts/ingest-fpl.js y scripts/reconcile.js (backend,
# bypassea RLS). NUNCA exponer SUPABASE_SERVICE_KEY al cliente/bundle
# (por eso no lleva prefijo VITE_).
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
